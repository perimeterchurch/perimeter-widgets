# Brand assets

Drop real logo-mark SVGs here and update `apps/site/src/app/design/branding/page.tsx` to reference them.

Expected filenames:

- `primary.svg` — full-color mark on light backgrounds
- `white.svg` — reversed (white fill) mark for dark backgrounds
- `mono.svg` — single-color mark for print / monochrome contexts
