// Minimal Vite config for shadcn CLI framework detection.
// The shared package is a library, not an app — this file exists
// solely so `pnpm dlx shadcn add` can detect a supported framework.
import { defineConfig } from 'vite';

export default defineConfig({});
