node_modules/
.turbo/
*.tsbuildinfo
.env
.env.local
.DS_Store
# dist/ is NOT ignored — committed for jsDelivr CDN serving
# But storyboard dist is a dev build, not for CDN
packages/storyboard/dist/
.superpowers/
