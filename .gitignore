node_modules/
.turbo/
*.tsbuildinfo
.env
.env.local
.DS_Store
# dist/ is NOT ignored — committed for jsDelivr CDN serving
# But storyboard and internal package dist/ are local build artifacts, not for CDN
packages/storyboard/dist/
packages/vite-preset/dist/
.superpowers/
.worktrees/
